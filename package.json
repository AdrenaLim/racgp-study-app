{
  "name": "racgp-study-app",
  "version": "1.0.0",
  "private": true,
  "description": "RACGP Fellowship exam study system - Cloudflare Workers + D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:local": "wrangler d1 execute racgp-study-db --local --file=schema.sql && wrangler d1 execute racgp-study-db --local --file=seed/seed.sql",
    "db:remote": "wrangler d1 execute racgp-study-db --remote --file=schema.sql && wrangler d1 execute racgp-study-db --remote --file=seed/seed.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
